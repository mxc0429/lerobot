# 项目文件说明

## 核心文件（必需）

### 程序文件

1. **src/lerobot/display/multi_camera_display.py**
   - 多相机显示核心模块
   - 提供OpenCV版本的多相机显示功能

2. **examples/so101_sim/multi_camera_display_mujoco.py**
   - 仿真测试程序（无需真实机械臂）
   - 使用MuJoCo渲染 + Matplotlib显示
   - 同时显示三个相机视角

3. **examples/so101_sim/multi_camera_teleop_matplotlib.py**
   - 真实机械臂遥操作程序
   - 支持校准文件加载
   - 高性能渲染

4. **examples/so101_sim/calibrate_leader_follower.py**
   - Leader-Follower校准工具
   - 建立精确的角度映射关系
   - 校准数据保存到 calibrations/ 目录

### 文档文件

5. **README_使用指南.md**
   - 唯一的使用文档
   - 包含所有必要的使用说明

### 工具脚本

6. **install_dependencies.bat**
   - 一键安装所有依赖
   - Windows系统使用

## 其他保留文件（原有）

### 示例程序

- `examples/so101_sim/teleop_record.py` - 数据录制程序

### 文档

- `examples/so101_sim/README.md` - 示例目录说明

## 已删除的冗余文件

### 文档（20+个）
- 各种重复的快速入门、解决方案、优化指南等文档

### 测试程序（6个）
- `multi_camera_display_test.py` - OpenCV版本测试
- `multi_camera_teleop.py` - OpenCV版本遥操作
- `multi_camera_test_mujoco.py` - MuJoCo原生窗口测试
- `multi_camera_teleop_mujoco_viewer.py` - MuJoCo原生窗口遥操作
- `sim_random_actions.py` - 随机动作测试
- `test_sim_setup.py` - 环境测试

### 文档（4个）
- `QUICKSTART.md`
- `IMPLEMENTATION_SUMMARY.md`
- `README_multi_camera_teleop.md`
- `快速入门.md`

### 工具脚本（2个）
- `test_opencv_gui.py`
- `fix_opencv.bat`

## 使用流程

### 1. 安装依赖

```bash
install_dependencies.bat
```

### 2. 测试程序

```bash
python examples/so101_sim/multi_camera_display_mujoco.py
```

### 3. 真实机械臂遥操作

```bash
python examples/so101_sim/multi_camera_teleop_matplotlib.py --port COM5
```

### 4. 校准（可选）

```bash
python examples/so101_sim/calibrate_leader_follower.py --port COM5 \
    --calibration_name my_robot
```

### 5. 使用校准文件

```bash
python examples/so101_sim/multi_camera_teleop_matplotlib.py --port COM5 \
    --calibration_file calibrations/my_robot.json
```

## 目录结构

```
lerobot/
├── src/lerobot/display/
│   └── multi_camera_display.py          # 核心模块
├── examples/so101_sim/
│   ├── multi_camera_display_mujoco.py   # 测试程序 ⭐
│   ├── multi_camera_teleop_matplotlib.py # 遥操作程序 ⭐
│   └── calibrate_leader_follower.py     # 校准工具 ⭐
├── calibrations/                         # 校准文件目录（自动创建）
├── README_使用指南.md                    # 使用文档 ⭐
├── install_dependencies.bat              # 安装脚本 ⭐
└── 项目文件说明.md                       # 本文件
```

⭐ 标记的是核心文件
