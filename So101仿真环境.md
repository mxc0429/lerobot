"录制视频使用"
python3 examples/so101_sim/teleop_record_block_stacking.py \
    --repo_id test/block_stacking_test \
    --num_episodes 1 \
    --port /dev/ttyACM0 \
    --leader_id my_awesome_leader_arm \
    --leader_calibration_dir ~/.cache/huggingface/lerobot/calibration/teleoperators/so101_leader \
    --skip_calibration \
    --display
快速测试（验证控制）
python3 examples/so101_sim/teleop_test_fast.py \
    --port /dev/ttyACM0 \
    --leader_id my_awesome_leader_arm \
    --duration 30 \
    --display


    # 基础使用（三相机）
python3 examples/so101_sim/teleop_with_camera_display.py \
    --port /dev/ttyACM0 \
    --leader_id my_awesome_leader_arm

# 只显示顶部相机（更快）
python3 examples/so101_sim/teleop_with_camera_display.py \
    --port /dev/ttyACM0 \
    --leader_id my_awesome_leader_arm \
    --single_camera

# 更高分辨率（较慢）
python3 examples/so101_sim/teleop_with_camera_display.py \
    --port /dev/ttyACM0 \
    --leader_id my_awesome_leader_arm \
    --camera_width 640 \
    --camera_height 480

# 同时显示MuJoCo窗口
python3 examples/so101_sim/teleop_with_camera_display.py \
    --port /dev/ttyACM0 \
    --leader_id my_awesome_leader_arm \
    --show_mujoco
